{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": ["./tsconfig.base.json", "./tooling/tsconfig.workspace-source.json"],
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["scripts/**/*.ts", "tests/**/*.ts", "vitest.config.ts"],
  "exclude": ["tests/fixtures/**"]
}
